using createBundleRenderer and put CSS and State
https://github.com/vuejs/vue-ssr-docs/blob/master/ja/api.md#レンダラオプション

---

install dependencies

``` bash
npm install
```

then build it

``` bash
npm build:server
```

then type

``` bash
npm start
```

then open http://localhost:8080 in your browser
