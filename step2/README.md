now we have universal js code. for both server and client.

- entry-client.js
- entry-server.js

are entry points. and both are import app.js which is the universal entry point.

---

install dependencies

``` bash
npm install
```

then build it

``` bash
npm build
```

then type

``` bash
npm start
```

then open http://localhost:8080 in your browser
